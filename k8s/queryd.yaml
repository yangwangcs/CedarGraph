# Deprecated placeholder only.
#
# QueryD execution is currently merged into GraphD. This file is intentionally
# not included by k8s/kustomization.yaml and must not create a Deployment,
# Service, or any pod that references the removed standalone QueryD binary.
apiVersion: v1
kind: ConfigMap
metadata:
  name: queryd-deprecated
  namespace: cedargraph
  labels:
    app.kubernetes.io/name: queryd
    app.kubernetes.io/component: query
    app.kubernetes.io/part-of: cedargraph
data:
  status: "deprecated"
  replacement: "GraphD handles query routing and distributed execution in this release."
