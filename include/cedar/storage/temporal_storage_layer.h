#pragma once

#include <memory>
#include <string>
#include <vector>
#include <cstdint>

namespace cedar {

// 前向声明
class CedarGraphStorage;
class Timestamp;
enum class AllenRelation;

// Temporal storage layer types and utilities placeholder.
// TemporalQueryEngine has been removed; use TMVEngine (cedar_gcn) instead.

} // namespace cedar
