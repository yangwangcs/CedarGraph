#include <gtest/gtest.h>
#include "cedar/dtx/storage_server.h"

using namespace cedar::dtx;

TEST(StorageServerStub, CompileAndLink) {
  // The storage_server.h header declares stub classes.
  // This test verifies the header compiles and links.
  EXPECT_TRUE(true);
}
